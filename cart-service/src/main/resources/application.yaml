server:
  port: 8081

spring:
  application:
    name: cart-service


  datasource:
    url: jdbc:postgresql://localhost:5432/cart_db
    username: mercora
    password: mercora

  flyway:
    enabled: true
    locations: classpath:db/migration

  logging:
    level:
      org.flywaydb: DEBUG

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true
